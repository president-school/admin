export interface ObjType {
    name: string;
    role: string;
    full_name: string;
    id?: number;
    img: string;
  }
  export type Inputs = {
    example: string
    exampleRequired: string
  }